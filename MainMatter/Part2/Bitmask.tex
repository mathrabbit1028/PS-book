\section{배열 대신 숫자 하나로}
크기 $N$의 \texttt{bool}형 배열을 처리해야 하는 상황을 생각해보자. 물론 배열 자체로도 임의 인덱스에 접근할 수 있기 때문에 충분히 효율적이지만 배열로 사용하는 것에는 한계가 있다. 함수에 배열을 넘기는 것은 추가 작업이 필요하고, 배열은 인덱스나 값 비교에 쉽게 사용될 수 없다. 이러한 문제를 해결할 수 있는 방법이 비트마스크이다.
\texttt{bool}형 배열의 $i$번째(0-based) 수를 2진법으로 나타내었을 떄 $2^i$ 자리수로 대응시킨 숫자를 생각하자. 그러면 이 숫자는 $2^N$보다 작은 음 아닌 정수가 될 것이다. 코딩에서 배열보다는 숫자가 훨씬 다루기 편하다는 점에서 비트마스킹은 큰 이점을 갖는다.

이제 \texttt{bool}형 배열과 숫자 사이의 전환을 해주는 코드를 살펴보자. \Cref{code: bitmask}을 보라.

\begin{code} \label{code: bitmask}
비트마스크 함수
\begin{lstlisting}
int boolToNum(int n, bool *arr) {
    int ret = 0;
    for (int i = 0; i < n; i++) {
        if (arr[i]) ret += (1<<i);
    }
    return ret;
}

bool numToBool(int x, int k) {
    return (bool)(x & (1<<k));
}

int setToOne(int x, int k) {
    return x | (1<<k);
}

int setToZero(int x, int k) {
    return x & (1<<k);
}
\end{lstlisting}
\end{code}

\Cref{code: bitmask}의 네 함수는 각각 다음 역할을 한다:
\begin{itemize}
    \item \texttt{boolToNum(n, arr)}: 길이 $n$의 \texttt{bool}형 배열을 $i$번째 값을 $2^i$자리수로 가지는 숫자로 변경
    \item \texttt{numToBool(x, k)}: 비트를 나타내는 수 $x$에서 $k$번째 비트 값 반환
    \item \texttt{setToOne(x, k)}: 비트를 나타내는 수 $x$에서 $k$번째 비트 값을 $1$로 변경
    \item \texttt{setToZero(x, k)}: 비트를 나타내는 수 $x$에서 $k$번째 비트 값을 $0$으로 변경
\end{itemize}

이 과정에서 비트 쉬프트, 비트 AND, 비트 OR, 비트 XOR 등의 비트 연산을 사용하는 것이 유용하다. 특히, $2^k$의 경우에는 \texttt{1<<k}로 간단하게 표현할 수 있음을 상기하자. 또한 비트 연산은 매우 빠르기 때문에 비트 연산을 사용하는 것이 프로그램의 수행 시간을 줄이는데 도움이 된다.

비트마스크는 주로 $N$개 각각을 선택하거나 선택하지 않은 상태를 쉽게 표현하기 위해 사용된다. 백트래킹으로 쉽게 해결할 수 있는 크기 $N$의 집합의 모든 부분집합을 출력하는 문제를 생각해보자. 백트레킹으로 풀 떄에는 레벨을 나타내는 변수 $L$을 현재 보는 원소의 인덱스로 잡고 재귀함수를 구현해야 한다. 하지만 비트마스킹을 쓰면 단순히 $i=0$부터 $i=2^N-1$까지의 모든 수에 대한 반복으로 쉽게 모든 부분집합을 출력할 수 있다(\Cref{pb: bitmask-subset}).

\section{\texttt{std::bitset}}
STD에서는 \texttt{bitset}이라고 불리는 비트를 저장하는 특별한 컨테이너(container)를 지원한다. \texttt{bitset}의 맴버 함수(member function)으로는 \texttt{all, any, none, flip, set, reset} 등이 있다. 예시 사용법은 \Cref{code: std-bitset}를 참고하라.

\begin{code} \label{code: std-bitset}
\url{https://en.cppreference.com/w/cpp/utility/bitset}
\begin{lstlisting}
#include <bitset>
#include <cassert>
#include <cstddef>
#include <iostream>
 
int main()
{
    typedef std::size_t length_t, position_t; // the hints
 
    // constructors:
    constexpr std::bitset<4> b1;
    constexpr std::bitset<4> b2{0xA}; // == 0B1010
    std::bitset<4> b3{"0011"}; // can also be constexpr since C++23
    std::bitset<8> b4{"ABBA", length_t(4), /*0:*/'A', /*1:*/'B'}; // == 0B0000'0110
 
    // bitsets can be printed out to a stream:
    std::cout << "b1:" << b1 << "; b2:" << b2 << "; b3:" << b3 << "; b4:" << b4 << '\n';
 
    // bitset supports bitwise operations:
    b3 |= 0b0100; assert(b3 == 0b0111);
    b3 &= 0b0011; assert(b3 == 0b0011);
    b3 ^= std::bitset<4>{0b1100}; assert(b3 == 0b1111);
 
    // operations on the whole set:
    b3.reset(); assert(b3 == 0);
    b3.set(); assert(b3 == 0b1111);
    assert(b3.all() && b3.any() && !b3.none());
    b3.flip(); assert(b3 == 0);
 
    // operations on individual bits:
    b3.set(position_t(1), true); assert(b3 == 0b0010);
    b3.set(position_t(1), false); assert(b3 == 0);
    b3.flip(position_t(2)); assert(b3 == 0b0100);
    b3.reset(position_t(2)); assert(b3 == 0);
 
    // subscript operator[] is supported:
    b3[2] = true; assert(true == b3[2]);
 
    // other operations:
    assert(b3.count() == 1);
    assert(b3.size() == 4);
    assert(b3.to_ullong() == 0b0100ULL);
    assert(b3.to_string() == "0100");
}
\end{lstlisting}
\end{code}

\section{연습문제 A}
\begin{problem}
    \Cref{code: bitmask}의 \texttt{BoolToNum(n, arr)}은 나머지 세 종류의 함수로 구현할 수 있다. \texttt{BoolToNum(n, arr)}를 나머지 세 종류의 함수로 구현하는 프로그램을 작성하라.
\end{problem}
\begin{problem}
    아래 함수 \texttt{setToOne, setToZero}에는 문제가 있다. 무엇이 문제인지 설명하라.
\begin{lstlisting}
int setToOne(int x, int k) {
    return x + (1<<k);
}
int setToZero(int x, int k) {
    return x - (1<<k);
}
\end{lstlisting}
\end{problem}
\begin{problem} \label{pb: bitmask-subset}
    비트마스크를 이용해 크기 $N$의 집합의 모든 부분집합을 출력하는 코드를 직성하라.
\end{problem}

\begin{problem}
    (26512번) 2의 보수 표현법은 음수를 이진법 숫자로 표현하기 위한 방식이다. 2의 보수 표현법은 첫 번째 자리를 부호 비트로 하고 남은 자리를 이용해 숫자를 표시하되, 남은 자리가 클수록 더 큰 숫자가 되도록 하는 것이다.
    예를 들어 8bit 자료형인 \texttt{char} 자료형을 생각해보자. 첫 번째 비트는 부호 비트이고, 나머지 7개의 비트는 값을 나타낸다. 부호가 결정된 상태에서 7개의 비트가 모두 0이면 가장 작은 수이고, 모두 1이면 가장 큰 수이다. 즉, $11111111_{(2)}$는 음수 중 가장 큰 수인 $-1$이고, 실제로 가장 작은 수인 $-128$을 표현하려면 $10000000_{(2)}$로 써야 한다.
    이제 정수 $X, Y$가 주어질 때, 8-bit 2의 보수 표현법으로 $X, Y, -X, -Y, X-Y$를 표현하는 코드를 작성하라. 다섯 개의 값 모두 $-128$ 이상 $127$ 이하임이 보장된다.
\end{problem}

\section{연습문제 B}

\begin{problem}
    (1062번) $N$개의 단어가 주어질 때, 26개의 알파벳 소문자 중 $K$개를 골라 그 문자들로만으로 이루어진 단어의 개수를 세자. 이 값의 최댓값을 출력하라. $N \leq 50$이고 문자열의 길이는 전부 $15$ 이하이다.
\end{problem}

\begin{problem}
    (1887번) $T$ 이하의 자연수 중 0개 이상의 수를 고른 집합이 주어진 $N$개의 집합을 포함하지 않도록 고르는 경우의 수를 출력하라. $T \leq 20$, $N \leq 52$이고, 주어진 집합은 공집합이 아닌 $\{ 1, 2, \cdots, T \}$의 부분집합임이 보장된다.
\end{problem}

\begin{problem}
    (6989번*) 다음과 같은 방식으로 점수를 산출하는 방법을 고려해보자.
    \begin{enumerate}
        \item $i$번 문항을 틀린 경우 $i$번 문항에 대한 점수는 $0$이다.
        \item $i$번 문항을 맞춘 경우 $i$번 문항에 대한 점수는
        \begin{itemize}
            \item $i>1$이고 $i-1$번 문항을 맞춘 경우, $i-1$번 문항에 대한 점수에 $i$번 문항에 지정된 특정 숫자 $S_i$를 더한 값이다.
            \item $i=1$이거나 $i-1$번 문항을 맞추지 못한 경우, $S_i$이다.
        \end{itemize}
        \item 총점은 각 문항에 대한 점수의 합이다.
    \end{enumerate}
    $S_i$가 주어질 때 총점으로 나올 수 없는 값들 중 $K$ 이상인 값들을 생각하자. 이 값들의 최솟값을 출력하라. $N \leq 150$, $1 \leq S_i \leq 100$, $1 \leq K \leq 2 \cdot 10^9$이다.
\end{problem}

\begin{problem}
    (8903번*) 다섯 개의 수로 이루어진 $N$개의 순서쌍 중, $K$개의 순서쌍을 고르자. 이 $K$개의 순서쌍의 $i$번째 각 성분의 최댓값을 $S_i$라고 할 때, $S_1+S_2+S_3+S_4+S_5$의 최댓값을 출력하라. $K \leq N \leq 10^4$이고 주어지는 $N$개의 순서쌍의 각 성분은 $0$ 이상 $10^4$ 이하의 정수이다.
\end{problem}

\begin{problem}
    (12833번) 세 수 $A, B, C$가 주어질 때, $A$에 $B$를 $C$번 bitwise XOR 연산한 결과를 출력하라. $A, B, C$는 모두 $0$보다 크고 $10^9$보다 작은 정수이다.
\end{problem}

\begin{problem}
    (15936번) $0$ 이상 $2^N$ 미만인 수들을 생각하자. 다음과 같은 규칙으로 수를 변경하려고 한다.
    \begin{itemize}
        \item 현재 수가 $0$ 이상 $2^N$ 미만인 수 $x$라면, $x < y < 2^N$이고 $x$와 $y$의 bitwise XOR 결과가 2의 거듭제곱꼴인 $y$를 반환한다.
    \end{itemize}
    $N, M, K$가 주어질 때,
    \begin{itemize}
        \item 한 번의 변경으로 $M$을 만들 수 있는 가장 큰 정수
        \item $M$에서 한 번의 변경을 만들 수 있는 가장 작은 정수
        \item 어떤 수 $a$에서 정확히 $K$번의 변경으로 $b$를 만들 수 있는 순서쌍 $(a, b)$의 개수를 $100\,003$으로 나눈 나머지
    \end{itemize}
    을 출력하라. $2 \leq K \leq N \leq 10^5$, $1 \leq M \leq \min(10^8, 2^N-1)$이고 답이 존재함이 보장된다.
\end{problem}

\begin{problem}
    (25152번*) $K$가 주어지면 $0$ 또는 $1$로만 구성된 $R \times C$ 크기의 이차원 배열을 출력해야 한다. 이떄, 출력된 배열은 왼쪽 상단에서 오른쪽 또는 아래 방향으로 $0$인 칸만을 통해 이동해 오른쪽 하단에 도달하는 서로 다른 경로의 수가 $K$인 배열이어야 한다. $R$과 $C$의 선택은 자유롭지만, 두 값 모두 $1\,000$ 이하여야 한다.
\end{problem}

\begin{problem}
    (9527번) $f(x)$를 $x$를 이진수로 표현했을 때 $1$의 개수라고 하자. $f(A)+f(A+1)+\cdots+f(B)$를 출력하라. $1 \leq A \leq B \leq 10^{16}$이다.
\end{problem}

\begin{problem}
    (26180번*) 원주 위에 $n$개의 점이 있고, 각 점에는 $0$ 또는 $1$의 숫자가 적혀 있다. 하지만, 여러분은 $n$이 얼마인지 모르고 다음 두 가지 동작을 $3n+500$회 이하로 사용하여 알아내어야 한다.
    \begin{itemize}
        \item 왼쪽 또는 오른쪽으로 인접한 점으로 이동한다.
        \item 현재 점에 적힌 숫자가 $0$이면 $1$로, $1$이면 $0$으로 바꾼다.
    \end{itemize}
    매 작업이 끝난 이후 현재 위치한 점에 적힌 숫자를 알 수 있다. 올바른 $n$의 값을 찾는 프로그램을 작성하라. $3 \leq n \leq 5000$이다. 이 문제는 인터랙티브 문제이므로, 입출력을 통한 인터렉션에 관한 세부사항은 마음대로 정해도 좋다.
\end{problem}

\section{연습문제 C}

\begin{problem}
    (2833번) $N \times N$ 크기의 체스판에서 다음 규칙으로 나이트를 $T$번 움직이려고 한다.
    \begin{itemize}
        \item 나이트는 체스의 기존 규칙에 따라 수평으로 한 칸, 수직으로 두 칸 떨어진 칸이나 수직으로 두 칸, 수평으로 한 칸 떨어진 칸으로 이동해야 한다.
        \item 나이트는 체스판 밖으로 움직일 수 없다.
        \item 현재 이동이 $i$번째 이동이라면, 나이트가 이동하려는 칸에 적힌 숫자는 \textbf{$i$의 배수}여야 한다.
    \end{itemize}
    체스판에 적힌 숫자들과 이동 횟수 $T$가 주어졌을 때 가능한 도착 위치의 개수를 출력하고, 도착 위치들을 행이 증가하는 순서로, 행이 같다면 열이 증가하는 순서로 출력하라. $3 \leq N \leq 30$, $1 \leq T \leq 10^6$이며, 체스판에 적힌 숫자는 $10^9$ 이하의 자연수이다.
\end{problem}

\begin{problem}
    (4207번) 그리고 $1 \leq i, j \leq n$이고 $i \neq j$인 모든 $(i, j)$에 대해 $i \rightarrow j$의 관계와 $j \rightarrow i$의 관계 중 \textbf{정확히 하나}가 존재한다. 여기서 관계는 다음을 의미한다:
    \begin{itemize}
        \item 만약 $a \rightarrow b$의 관계가 존재한다면, $a$를 골랐을 때 자동으로 $b$도 선택된다. 하지만, $b$를 골랐을 때 $a$가 선택되지 않는다.
    \end{itemize}
    $1$부터 $n$까지의 자연수 중 최소 개수를 골라 모든 자연수가 선택되도록 할 때, 최소 개수를 찾고, 최소 개수로 고르는 실례를 \textbf{아무거나} 하나 찾아라. $1 \leq n \leq 75$이다.
\end{problem}

\begin{problem}
    (13169번) $n$개의 수 $A_1, A_2, \cdots, A_n$과 집합 $N = \{ 1, 2, \cdots, n \}$에 대해, $\oplus$를 bitwise XOR 연산이라고 하면
    $$S_I = \sum_{k \in I \subseteq N} {A_k}$$
    $$X = \bigoplus_{I \subseteq N} {S_I}$$
    이다. $X$를 구하는 프로그램을 작성하라. $1 \leq n \leq 30$, $0 \leq A_i < 2^{30}$이다.
\end{problem}

\begin{problem}
    (17458번**) 전체집합 $U = \{ 0, 1, \cdots, 2^k-1 \}$와 공집합이 아닌 부분집합 $A \subseteq U$를 생각하자. 다음 조건들을 만족하는 집합을 \textit{좋은 집합}이라고 하자:
    \begin{itemize}
        \item 모든 $x, y \in A$에 대해 $x$와 $y$의 bitwise AND 연산의 결과도 $A$에 속한다.
        \item 모든 $x, y \in A$에 대해 $x$와 $y$의 bitwise OR 연산의 결과도 $A$에 속한다.
    \end{itemize}
    $[0, 2^k-1]$에 속하는 $n$개의 서로 다른 정수가 주어질 때, 이 정수들을 모두 포함하는 \textit{좋은 집합} $A \subseteq U$의 개수를 출력하라. $1 \leq k \leq 7$, $0 \leq n \leq 2^k$이다.
\end{problem}

\begin{problem}
    (17752번) 두 플레이어 A와 B는 협동하여 A가 들은 수 $X$를 B에게 전달해야 한다. 이때, $4 \times 4$ 크기의 게임판을 통해서만 전달할 수 있다. 이 과정은 다음과 같이 진행된다.
    \begin{itemize}
        \item 게임의 진행자 K는 게임판의 임의의 위치에 말을 하나 놓는다. 이어서, 길이 $10,000$의 \texttt{A}와 \texttt{B}로만 이루어진 문자열에 적힌 순서로 A와 B를 부른다.
        \begin{itemize}
            \item 이 문자열에는 같은 문자가 연속하여 많아야 $100$번 등장한다.
        \end{itemize}
        \item A는 자신이 불려갔을 때, 현재 게임판에 놓인 말의 위치를 보고 상하좌우로 인접한 칸으로 옮긴다. 당연히 게임판 밖으로는 움직일 수 없다.
        \item B는 자신이 불려갔을 때, 현재 게임판에 놓인 말의 위치를 보고 예상되는 $X$의 값을 외치거나 상하좌우로 인접한 칸으로 옮긴다. \begin{itemize}
            \item 만약 $X$의 값을 외친 경우, 실제 $X$의 값을 맞추었다면 두 플레이어의 승리로 종료되고, 맞추지 못하였다면 두 플레이어의 패배로 종료된다.
            \item 말을 옮긴 경우에는 계속 진행된다. 당연히 게임판 밖으로는 움직일 수 없다.
        \end{itemize}
    \end{itemize}
    A와 B의 전략을 나타내는 코드를 작성하라. 추가로, A는 서브테스크 번호 $T$와 숫자 $X$를 듣고 전략을 구상할 수 있으며, B도 서브테스크 번호 $T$를 듣고 전략을 구상할 수 있다. 이 문제는 투 스탭 문제이므로, 함수에 관한 세부사항은 본문을 참고하라.
    \begin{center}
    \begin{tabular}{ | m{0.1\textwidth} | m{0.1\textwidth} | m{0.6\textwidth} | } 
        \hline
        번호 & 배점 & 추가 제약 조건 \\ 
        \hline
        1 & 20\% & $T=1$이고 주어지는 문자열은 \texttt{ABAB...}과 같이 \texttt{AB}가 반복되는 형태이다. \\ 
        \hline
        2 & 20\% & $T=2$이고 처음 말의 위치는 $(1, 1)$임이 보장된다. \\ 
        \hline
        3 & 60\% & $T=3$이고 추가 제약 조건은 없다. \\
        \hline
    \end{tabular}
    \end{center}
\end{problem}

\begin{problem}
    \texttt{A}부터 \texttt{Z}까지 이름이 정해진 26개의 레지스터가 있고, 각 레지스터는 64-bit의 부호 없는 정수를 저장할 수 있다. 또 다음 일곱 개의 레지스터 연산이 있다.
    \begin{itemize}
        \item \texttt{mov $R$ $X$}: $X$에 저장된 값을 $R$에 저장한다.
        \item \texttt{and $R$ $X$}: $X$에 저장된 값과 $R$에 저장된 값을 bitwise AND 후, $R$에 저장한다.
        \item \texttt{or $R$ $X$}: $X$에 저장된 값과 $R$에 저장된 값을 bitwise OR 후, $R$에 저장한다.
        \item \texttt{xor $R$ $X$}: $X$에 저장된 값과 $R$에 저장된 값을 bitwise XOR 후, $R$에 저장한다.
        \item \texttt{not $R$}: $R$에 저장된 값을 bitwise NOT 후, $R$에 저장한다.
        \item \texttt{shl $R$ $X$}: $R$에 저장된 값을 왼쪽으로 $X$ bit shift한 후, $R$에 저장한다. 남는 칸은 0으로 채워진다.
        \item \texttt{shr $R$ $X$}: $R$에 저장된 값을 오른쪽으로 $X$ bit shift한 후, $R$에 저장한다. 남는 칸은 0으로 채워진다.
    \end{itemize}
    (a) (18716번) \textbf{[문제 1]}를 해결하는 레지스터 연산을 나열하라. 레지스터 연산의 수는 $64$회 이하여야 한다.
    \begin{tcolorbox}[colback=white, colframe=black, sharp corners, boxrule=0.5pt]
        \textbf{[문제 1]}
        레지스터 \texttt{A}에 임의의 입력 숫자 $x$가 주어져있고 다른 칸들에는 0이 저장되어 있다. \texttt{A}에 저장된 값을 $8$만큼 증가시켜라(modulo $2^{64}$으로). 즉, 레지스터 \texttt{A}에 $x+8$을 저장하라. 나머지 레지스터에 저장된 값은 무엇이던 상관이 없다.
    \end{tcolorbox}
    (b) (18717번) \textbf{[문제 2]}를 해결하는 레지스터 연산을 나열하라. 레지스터 연산의 수는 $300$회 이하여야 한다.
    \begin{tcolorbox}[colback=white, colframe=black, sharp corners, boxrule=0.5pt]
        \textbf{[문제 2]}
        레지스터 \texttt{A}에 임의의 입력 숫자 $x$가 주어져있고 다른 칸들에는 0이 저장되어 있다. 이진법 표현에서 $x$와 같은 개수의 $1$을 가지고 있는 $x$보다 큰 가장 작은 수를 $y$라고 하자. 예를 들어 $x=23$이면 $y=27$이다. $y$를 레지스터 \texttt{A}에 저장하라. $y$는 $2^{64}$ 미만의 값임이 보장되고, 나머지 레지스터에 저장된 값은 무엇이던 상관이 없다.
    \end{tcolorbox}
\end{problem}

\begin{problem}
    (18913번) 임의의 두 $(i, j)~(1 \leq i \neq j \leq n)$에 대해 $i \rightarrow j$의 간선과 $j \rightarrow i$의 간선 중 \textbf{정확히 하나}가 존재하는 유향 그래프가 주어진다. $n \leq 3,000$에서 그래프의 간선을 14개 이하의 색으로 컬러링하여 모든 $i \rightarrow j \rightarrow k~(i \neq k)$ 경로의 두 간선의 색이 다르도록 할 수 있음이 보장되어 있다. 가능한 컬러링을 아무거나 출력하라.
\end{problem}

\begin{problem}
    (19674번) $1$부터 $N$까지의 수가 섞인 길이 $N$의 순열 $A$가 있으나, 여러분은 $A$의 각 원소를 알 수 없다. 여러분은 그레이더와 다음 기능을 하는 함수를 통해 상호작용하여 $A$의 각 원소를 알아내야 한다.
    \begin{itemize}
        \item $1$부터 $N$까지의 수를 $K$개씩 총 $B$개의 버킷으로 분류해 함수의 인자로 호출한다. $N = B \times K$임이 보장된다.
        \item 그러면 함수에서는 각 버킷에 포함된 수의 인덱스에 있는 수 $K$개를 반환한다. \textbf{단, $K$개의 수로 묶인 $i$번째 묶임이 $i$번째 버킷에 있었던 수들임을 보장하지 않는다.} 즉, 함수의 인자에서의 버킷 순서와 반환된 배열에서의 버킷 순서는 다를 수 있다. \textbf{또, 한 묶임에서도 $j$번째 수가 그 버킷에서 $j$번째에 있었던 수임을 보장하지 않는다.} 즉, 함수의 인자에서 각 버킷의 원소 순서와 반환된 배열에서 각 버킷의 원소 순서는 다를 수 있다.
        \begin{itemize}
            \item 예를 들어 $[1, 2, 3]$, $[4, 5, 6]$의 두 개의 버킷을 만들었고, $A = [2, 4, 5, 3, 1, 6]$이면 
            
            $[[3, 6, 1], [5, 4, 2]]$은 가능한 하나의 반환 배열이다.
            \item 반환 배열의 첫 번째 묶임은 입력에서 두 번째 버킷이었던 수들의 인덱스에 존재하는 수 $[3, 1, 6]$이 임의의 순서로 섞여 있다.
            \item 반환 배열의 두 번째 묶임은 입력에서 첫 번째 버킷이었던 수들의 인덱스에 존재하는 수 $[2, 4, 5]$가 임의의 순서로 섞여 있다.
            \item 용어를 \textit{버킷}과 \textit{묶임}으로 다르게 쓰고 있음을 상기하라.
        \end{itemize}
    \end{itemize}
    이 함수를 $Q$번 이하로 호출하여 순열 $A$를 알아내어라. $B \geq 2$, $K \geq 2$, $6 \leq N \leq 1,000$, 그리고 $N = B \times K$이며, $Q$는 하단의 서브테스크를 참고하라.
    \begin{center}
    \begin{tabular}{ | m{0.1\textwidth} | m{0.1\textwidth} | m{0.6\textwidth} | } 
        \hline
        번호 & 배점 & 추가 제약 조건 \\ 
        \hline
        1 & 2\% & $N=6$, $B=2$, $K=3$, $Q=100$, $ST=1$ \\ 
        \hline
        2 & 3\% & $N=6$, $B=3$, $K=2$, $Q=100$, $ST=2$ \\ 
        \hline
        3 & 12\% & $Q=12$, $ST=3$, 그레이더 함수 \texttt{randomResult}는 \textbf{버킷을 섞지 않는다.} 여전히 \textbf{각 버킷의 숫자는 섞인다.} \\ 
        \hline
        4 & 16\% & $K=2$, $Q=4$, $ST=4$ \\ 
        \hline
        5 & 15\% & $B=2$, $Q=12$, $ST=5$ \\ 
        \hline
        6 & 52\% & $B>2$, $K>2$, $Q=2000$, $ST=6$, 각 테스트케이스에서 \textbf{$Q$의 최댓값 $Q_{\max}$에 따라 점수가 결정}된다. 
        \vspace*{1em}
        \begin{center}
        \begin{tabular}{ | m{0.3\textwidth} | m{0.2\textwidth} |} 
            \hline
            $Q_{\max}$의 조건 & 획득 점수 \\ 
            \hline
            $Q_{\max} > 2000$ & 0\% \\ 
            \hline
            $500 < Q_{\max} \leq 2000$ & 8\% \\ 
            \hline
            $50 < Q_{\max} \leq 500$ & 17\% \\ 
            \hline
            $9 < Q_{\max} \leq 50$ & $22+30 \times \left( \frac{50-q}{41} \right)^2$\% \\ 
            \hline
            $Q_{\max} \leq 9$ & 52\% \\ 
            \hline
        \end{tabular}
        \end{center} \\ 
        \hline
    \end{tabular}
    \end{center}
\end{problem}

\begin{problem}
    (21768번) 길이 $2^N$의 음이 아닌 정수로 이루어진 수열 $A_0, A_1, \cdots, A_{2^N-1}$을 생각하자. $0 \leq i, j \leq 2^N-1$이며 $A_i + A_j < A_{i \land j} + A_{i \lor j}$인 $(i, j)$가 존재한다면 아무거나 하나 찾고, 존재하지 않는다면 \texttt{-1}을 출력하라. $\land$는 bitwise AND, $\lor$은 bitwise OR 연산자를 의미하고, $0 \leq N \leq 20$, $0 \leq A_i \leq 10^7$이다.
    \begin{center}
    \begin{tabular}{ | m{0.1\textwidth} | m{0.1\textwidth} | m{0.6\textwidth} | } 
        \hline
        번호 & 배점 & 추가 제약 조건 \\ 
        \hline
        1 & 14\% & $N \leq 14$ \\ 
        \hline
        2 & 17\% & $N \leq 17$ \\ 
        \hline
        3 & 69\% & 추가 제약 조건은 없음 \\
        \hline
    \end{tabular}
    \end{center}
\end{problem}

\begin{problem}
    (21860번) 수열 $A_1, A_2, \cdots, A_N$이 주어질 때, bitwise XOR 연산 $\oplus$에 대해
    $$\bigoplus_{1 \leq i \leq j \leq N} {A_i + A_j}$$
    의 값을 구하라. $1 \leq N \leq 10^6$, $1 \leq A_i \leq 5 \times 10^8$이다.
\end{problem}

\begin{problem}
    (21915번) $25$개의 칸이 $9$개의 빨간 칸, $8$개의 파란 칸, $7$개의 흰색 칸, $1$개의 특수 칸으로 이루어져 있다. 그리고 각 칸에는 좌상단부터 \texttt{a}부터 \texttt{y}까지 알파벳 라벨이 있다. 

    \begin{center}
        \includegraphics[width=0.8\textwidth]{Images/21915.png}
    \end{center}
    
    이제 단어 $w$와 자연수 $g~(1 \leq g \leq 9)$를 고르는 것을 반복할 것이다. 고를 때마다 다음 일이 발생한다.
    \begin{itemize}
        \item $w$를 이루는 각 알파벳을 앞에서부터 보며 그 알파벳에 대응하는 칸이 선택된 적이 없다면 남기고, 그렇지 않다면 지운다. \texttt{z}의 경우에는 지운다.
        \item 남은 알파벳 중 $g$번째 알파벳에 해당하는 칸을 선택한다. $g$번째 알파벳이 없거나 $g$번째 알파벳에 해당하는 칸이 빨간 칸이 아니라면 틀린다.
    \end{itemize}
    우리의 목표는 \textbf{계속 틀리지 않고} 모든 빨간 칸을 선택하는 것이다. $n$개의 길이 $30$ 이하의 단어 목록이 주어질 때, $q$개의 $25$개의 칸의 배치와, 각 칸이 선택되었는지의 여부가 주어질 때마다 목표를 달성하기 위한 $w$와 $g~(1 \leq g \leq 9)$의 쌍을 순서대로 출력하라. 만약 없다면 \texttt{IMPOSSIBLE}을 출력하면 된다. $1 \leq n \leq 10^5$, $1 \leq q \leq 10^5$이다.
\end{problem}

\begin{problem}
    (24068번) $N \times M$ 크기의 격자에 다음 규칙을 만족하도록 음이 아닌 자연수를 채워넣자.
    \begin{itemize}
        \item 상하좌우로 인접한 두 칸에 적힌 수를 $2$진법 표현했을 때 값이 서로 다른 비트가 하나 이하여야 한다.
        \item 모든 칸에 적힌 수는 달라야 한다.
    \end{itemize}
    이렇게 채우는 방법 중에서 최댓값이 최소가 되는 배치를 하나 출력하라. $1 \leq N, M \leq 2,000$이다.
\end{problem}

\begin{problem}
    (27511번) $N$명에서 다음 세 단계로 진행되는 게임을 하려고 한다.
    \begin{itemize}
        \item 1단계:
        \begin{itemize}
            \item $N$개의 자리에 있는 원탁에 $0$부터 $N-1$까지의 수가 적힌 종이 $N$장이 하나씩 놓여 있다.
            \item 각 자리에 정해진 순서로 한 명씩 앉아 자신과 자신의 \textbf{오른쪽} 사람이 가진 종이를 보고 $0$ 이상 $10^9$ 미만의 새로운 숫자를 적는다.
            \item 원래 있었던 종이는 각자 가져가 버린다. 그러면 \textbf{1단계와 관련된 기억이 지워진다.}
        \end{itemize}
        \item 2단계:
        \begin{itemize}
            \item 각 자리에 정해진 순서로 한 명씩 앉아 자신과 자신의 \textbf{왼쪽과 오른쪽} 사람이 가진 종이를 보고 $0$ 이상 $10^9$ 미만의 새로운 숫자를 적는다.
            \item 1단계에서 놓았던 종이는 각자 가져가 버린다. 그러면 \textbf{2단계와 관련된 기억이 지워진다.}
        \end{itemize}
        \item 3단계:
        \begin{itemize}
            \item 각 자리에 정해진 순서로 한 명씩 앉아 자신과 자신의 \textbf{왼쪽과 오른쪽} 사람이 가진 종이를 보고 $0$ 이상 $10^9$ 미만의 새로운 숫자를 적는다.
            \item 2단계에서 놓았던 종이는 각자 가져가 버린다. 그러면 \textbf{3단계와 관련된 기억이 지워진다.}
        \end{itemize}
    \end{itemize}
    이렇게 하여 3단계에서 놓은 종이를 전부 펼쳤을 때 인접한 두 자리에 적힌 숫자가 다르면서 모든 숫자가 $3$보다 작도록 하고 싶다. 이를 위한 플레이어들의 전략을 작성하라. $10 \leq N \leq 100,000$이다.
\end{problem}

\begin{problem}
    (27906번) 다음과 같은 규칙의 모자 게임을 생각하자.
    \begin{itemize}
        \item $N$명의 플레이어가 있고 한 명의 진행자가 있다. 
        \item 각 플레이어는 일부 플레이어의 모자만 볼 수 있다. 특히, 자기 자신의 모자는 절대 볼 수 없다.
        \item 모든 플레이어는 각 플레이어가 볼 수 있는 모자들을 알고 있다.
        \item 각 플레이어가 눈을 감으면 게임의 진행자가 각 플레이어에게 흰색 모자와 검은색 모자 중 하나를 씌운다.
        \item 이후 "흰색 모자를 $w_1$개 이상 $w_2$개 이하로 씌웠습니다"라고 말한다.
        \item 진행자가 몇 명의 플레이어에게 "당신이 쓴 모자의 색을 알고 있나요?"라고 $Q$번 질의한다. 그들은 동시에 대답해야 하며, 모두가 대답을 들을 수 있다.
    \end{itemize}
    모든 출제자가 완벽히 논리적으로 사고하며 근거 없는 추측을 하지 않는다고 가정할 때, 각 질의에서 각 출제자의 대답을 알아내어라. $1 \leq N \leq 20$, $1 \leq Q \leq 30$이다.
\end{problem}